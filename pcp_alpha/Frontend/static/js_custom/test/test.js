/*
pcp_custom_jquery unit testing

mocha js example of a test template:
var assert = chai.assert;

describe('Array', function() {
  it('should start empty', function() {
    var arr = [];

    assert.equal(arr.length, 0);
  });
});


*/

// ##### Test's for W1 #####
// test filter_w1 filtering out targets in the search bar
// test drag_target draging a target

// ##### Test's for W2 #####
// test get_commands_func populates a list of commands on W2 when user clicks on target
// test drag_command dragging command

// ##### Test's for W3 #####
// test add_new_job creating new job rows
// test delete_job_from_w3 deleting job row
// test clear_new_jobs deleting all existing job rows

// test add_target_to_job_sc_button
// test drop_target dropping target(s) dragging to job row(s)

// test add_command_to_job_sc_button
// test drop_command_to_multiple drop one command to all job rows

// test set_w3_job_status job status on a specific job row(s)

// test add_sequence_tab
//      synchronize_job_sequence_tabs

// test save_job_state save job rows in W3, load job rows in W3 from saving
//      load_job_state

// test execute_sequence self explained

// ##### Test's for W4 #####
// test execute_sequence_output
//      render_job_output_to_page self explained

